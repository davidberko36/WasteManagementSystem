from django.urls import path
from .views import customer_signup, driver_signup


# urlpatterns=[
#     path('signup/', driver_signup.as_view(), name='signup'),
#     path('createdriver/', driver_signup.as_view(), name='driversignup')
# ]